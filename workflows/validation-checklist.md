# 验证清单通用流程

## 最低要求

改动代码后，要做与影响范围匹配的验证。

- 后端改动：运行相关测试、服务启动检查、`py_compile` 或 import 校验。
- 前端改动：运行项目约定的构建、类型检查或测试。
- 数据库改动：检查迁移头、迁移升级和数据库文档。
- 接口联动：检查前后端字段、筛选条件、分页参数和权限口径。
- 配置改动：说明环境变量、默认值和部署同步范围。

## 不能验证时

如果因为环境、依赖、权限或外部服务无法验证，要明确说明：

- 哪个命令或步骤没能执行。
- 失败原因。
- 已经完成的替代检查。
- 后续需要谁在什么环境补验。
