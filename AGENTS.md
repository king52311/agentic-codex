## 规则

- `agentic-codex` 项目只要有文件更新，就立即执行 `git add`、`git commit`，不用等待。
- 提交信息使用中文。
- 前端项目页面及弹窗中涉及日期时间的展示和输入，默认统一使用 `YYYY-mm-dd h:i:s` 格式。
- 凡是处理 `agentic-codex` 相关工程项目的开发问题，都要把已验证有效的排查思路、修复方法、经验结论及时沉淀到公共规则中，持续积累，降低后续 bug 风险。
