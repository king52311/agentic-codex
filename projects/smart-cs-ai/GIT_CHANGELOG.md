# Git 变更记录

## 2026-08-22

- 财务报销助手主内容区改为全宽自适应，移除 `max-w-7xl` 限制
- 补充业务工具页、列表页、后台管理页默认自适应宽度规则
- 更新前端构建压缩包 `frontend/dist.zip`

## 2026-07-24

- 税务助手发票管理新增筛选区：发票类型、审核状态、合同编号、关键字，支持搜索和重置
- 去掉“仅看刘羽丰当前节点”开关，默认显示未审核
- 发票列表和详情增加审核状态，按 `invoice_no_info` 是否为空显示未审核/已审核
- 修正税务助手数据库字典中发票主表、明细表字段数

## 2026-06-27

- 新增独立爬虫模块、任务管理、详情调试执行能力
- 新增站点级配置目录与 `kehu51` 站点模板/格式化逻辑
- 补通 `kehu51` 跟进记录与客户列表解析
- 新增 `kehu51` 两张业务采集表，并已支持独立表优先落库
- 新增 SQL 同步脚本，支持同时执行 `test/prod`
- 客户列表任务已切换为 API 抓取
- 分页已支持 `page_end < 0` 时持续抓取到无数据为止

## 2026-07-03

- 修复企微模板管理接口因 `wecom_message_templates.template_type` 缺失导致的 500
- 已同步补齐 `template_type` 到 test / prod 两套环境，并回填历史模板为 `single_user`
- 新增单用户企微模板公共发送能力：按 `template_id + touser + template_vars` 调用
- 模板管理已增加模板类型筛选、ID 列、状态快捷启停
- 推送机器人管理已增加状态快捷启停

## 2026-07-08

- 业务团队管理看板新增 `其他非业务人员` 虚拟组，来源为张涛和 OA 售后服务部技术组人员
- `其他非业务人员` 在个人排名中聚合为单行，固定排最后，不参与其它个人统计图表
- 小组战况新增 `其他非业务人员` 行，仅展示年度完成，其它列显示 `-`
- 调整看板展示：小组战况高度、业绩目标整数展示、非业务人员拜访量显示 `-`
- 排除米鸿旭、赵成宇，不再纳入 `其他非业务人员`
- 看板人员缓存 key 升级到 `after_sales:dashboard_team_members:v4`

## 2026-07-13

- 新增人事考勤助手：企业微信考勤日汇总、人员汇总、详情弹窗、搜索/重置与同步能力
- 接入 OA 休假、考勤修正流程，补齐外出、事假、病假、迟到/早退、考勤修正等字段
- 新增钉钉考勤模块：钉钉人员同步、启用/禁用管理、考勤打卡记录落库
- 钉钉同步限定“研发中心”及子部门，支持配置排除人员名单
- 钉钉人员与打卡记录均自动匹配企微 `userID`，后续人员对比统一使用企微 userID
- 两套环境已写入钉钉考勤应用配置并执行建表 SQL
- 人事考勤助手新增“钉钉考勤人员管理”tab，展示企微用户ID并隐藏手机号
- 企微考勤汇总主列表隐藏外出、考勤修正和迟到/早退分段，详情弹窗保留完整字段
- 企微考勤汇总搜索改为人员级汇总搜索，列表和统计卡隐藏应出勤
- 修复客户无忧长跟进内容导致 `source_dedupe_key` 超长写入失败

## 2026-07-14

- 人事考勤助手支持花名册模板上传填充，新增年假、带薪病假、调休、产假、陪产假、婚假、丧假、哺乳假等请假类型
- 实际出勤口径调整为仅扣减事假与带薪病假，病假/事假保留一位小数
- 新增“提醒请假”tab：仅展示存在缺卡异常人员，按企微缺卡统计旷工和未打卡
- 人员别名/同名账号在考勤汇总中按同一人合并，实际出勤按同人同日最多 1 次计算
- “招标信息公示”拆分独立权限 `app:okcis_notices:access`，卡片、路由、应用中心与后端接口均已接入
- 晨天润达业务团队看板企微机器人推送增加超时重试：30 秒、60 秒重试两次，并增强任务失败 traceback 日志
- 招标信息公示新增跟进人记录和当前用户删除记录：列表展示跟进人、支持跟进人筛选、详情展示跟进时间和删除时间
- 招标信息公示删除按钮保持“删除”文案，但仅对当前登录用户隐藏记录；操作列按钮保持单行不换行
- 人事考勤助手缺卡统计补强钉钉覆盖企微口径：同人同日有钉钉记录时，汇总和缺卡详情均不再展示企微别名行
- 企微用户别名发送统一走主账号：`kangpeng` 归一为 `tangpeng`，企业微信单用户消息发送前自动归一并去重
- 人事考勤助手 userid 别名展示改为只显示主 userid，批量提醒按页面展示的 userid 发送
- 招投标群发计划任务拆分为两条：售后机器人 ID=2、售前机器人 ID=3，均为周一到周五 16:10
- OKCIS 订阅组 dzid 解析兼容数组和对象两种返回结构，并缓存订阅组明细
- 招标信息公示列表筛选改为后端生效，新增标题关键字筛选、分页和 Excel 导出
- 修复 OKCIS 验证码算式解析，支持 `5++37` 这类重复运算符并增加验证码异常重试

## 2026-07-15

- 用户列表部门筛选支持选择大部门，后端自动包含全部子部门用户
- 清理未使用 RBAC 角色 `yxzx`，test/prod 均确认无人绑定后删除
- 人事考勤助手“提醒请假 / 异常考勤”隐藏企微ID列，搜索提示同步去掉企微ID
- 异常考勤改为日期正序，同日按部门、姓名稳定排序
- 异常考勤分钟数显示为中文时长，地点异常模式分钟数显示 `-`
- 异常考勤详情中异常打卡明细标红
- 缺卡详情仅展示缺卡类型：上班缺卡、下班缺卡；无明细时显示“无打卡记录”
- 缺卡详情按钉钉覆盖企微口径判断方向：有钉钉明细时只用钉钉明细反推缺卡方向
- “企微考勤汇总”tab 仅管理员可见，非管理员访问该 tab 自动切到“提醒请假”
- 部门筛选下拉选项改为已加载部门全集，清空后不再停留在旧搜索结果部门列表
- 新增“企微&钉钉考勤同步”计划任务，每天 03:50 按页面同步按钮同样口径同步考勤，并写入 test/prod 计划任务元数据
- 提醒规则文案调整为“上班、下班都缺卡，如未走请假手续，记旷工 1 次”
- 人事考勤助手权限拆分为独立角色 `wecom_attendance_viewer`，从普通用户移除考勤权限，仅绑定 `ZhouYue`、`wangyamei`
- 管理后台“权限定义”“角色与权限”改为表格列表形式，替换原卡片网格布局
- 人事考勤助手新增“考勤配置”tab，拆分为时间设置、休息日、人员白名单三个子 tab
- 时间设置支持未来日期配置上班/下班参照时间，异常考勤按配置时间判断
- 休息日支持未来月份月历多选，批量保存后全员考勤正常
- 人员白名单支持搜索多选，白名单人员全局考勤正常
- 新增考勤特殊日期和白名单配置表，并已同步 test/prod 建表
- 人事考勤助手人员白名单列表新增删除功能，删除后按剩余名单覆盖保存并刷新考勤数据
- 更新前端构建压缩包 dist.zip

## 2026-07-16

- 修复人事考勤助手缺卡统计误判：缺卡/未打卡明细不再被当成已打卡，并重算 test/prod 数据
- 每日考勤详情弹窗去掉“钉钉记录”列
- 招标信息公示新增公告详情本地缓存字段和补抓脚本，`crawler_task_3` 列表爬取时同步抓取详情
- 招标详情预览优先读取本地缓存，未命中时使用 OKCIS 登录态抓取移动端 JSON 并回写缓存
- 招标信息公示页面增加预览弹窗、移动端适配、正文段落/表格/加粗等基础样式
- 正式环境现有公告详情已补抓 111/113，剩余 2 条为源站脱敏或接口 502
- 修复客户无忧导出汇总中同一客户保留短名的问题，优先展示完整客户名
- 更新前端构建压缩包 dist.zip

## 2026-07-16

- 人事考勤助手“异常考勤”新增导出按钮，按当前筛选导出部门、姓名、日期、异常打卡类型、状态、打卡时间、地点
- 人事考勤助手“缺卡考勤”新增导出按钮，按当前筛选导出部门、姓名、日期、打卡类型缺卡
- 原“提醒请假”tab 文案改为“缺卡考勤”，同步调整导出工作表名和文件名
- 缺卡同一天上班、下班都缺卡时拆成两行导出；异常考勤同一天多条异常按明细逐行导出
- 更新前端构建压缩包 dist.zip

## 2026-07-16

- 人事考勤助手“企微考勤汇总”更名为“考勤汇总”
- 考勤汇总统计前统一套用考勤配置
- 休息日不纳入出勤、缺勤、缺卡、迟到统计
- 白名单人员不计缺勤、缺卡、迟到，按正常出勤处理
- 时间设置按配置的上下班参照时间重新计算迟到/早退分段
- 更新前端构建压缩包 dist.zip

## 2026-07-16

- 人事考勤助手新增“排除部门”配置，企微考勤同步和历史列表按排除部门、人员白名单统一过滤
- 人员白名单从考勤人员范围中整体排除，不再参与汇总、详情和缺卡重算
- 新增“考勤修正”tab，展示 OA 已通过请假和考勤修正记录
- 考勤修正按“天”计入每日考勤，实际出勤每天最高 1 天，已有 1 天出勤时不再重复增加
- `actual_attendance`、`attendance_correction_count` 改为 `DECIMAL(6,1)`，支持 0.5 天等小数天
- 同步 test/prod 字段变更 SQL：`alter_wecom_attendance_actual_decimal.sql`、`alter_wecom_attendance_correction_decimal.sql`
- 更新前端构建压缩包 dist.zip

## 2026-07-16

- 修复客户无忧客户列表同步最后一页多计数问题
- `kehu51_customer_list` 在已知源站总数时按剩余条数裁剪当前页 `items`
- 避免正式环境王贺源站 14 条、接口返回 50 条导致 `source_total=14, crawled_total=50` 校验失败
- 新增 `[DATA-TRIM]` 日志，展示源站剩余条数和裁剪数量

## 2026-07-16

- 部门架构页面改为只读，保留查看与展开收起，提示“同步OA部门架构数据”
- OKCIS 爬虫手动执行支持传 `dzid`，指定时只抓对应订阅组，不传仍抓全部组
- OKCIS multipart raw body 分页参数支持随页码更新，避免重复抓第一页
- OKCIS 详情返回权限不足时自动刷新登录重试；重试仍不可见则跳过并清理旧数据
- 正式环境已单独执行售前营销 `dzid=187653`，确认权限不足记录不写库
- OKCIS 爬虫任务 3 定时改为每天 15:05:00，两套环境同步更新 `crawler_tasks` 与 `scheduler_job_meta`
- OKCIS 详情访问增加节流：每 10 次详情访问随机暂停 3 到 10 秒
- 招投标群发推送在当天数据为空时写入明确摘要，计划任务列表可看到未发送原因
- 更新前端构建压缩包 dist.zip

## 2026-07-16

- 角色与权限列表按角色名升序排序
- 角色与权限页面“权限数量”支持悬浮查看权限名列表，每行一个
- 用户列表单用户分配角色弹窗新增角色搜索，输入即过滤，支持一键清空
- 修复人事考勤助手考勤配置时间回显，兼容 `PT14H40M` 这类时间格式
- 考勤修正表头“时长”改为“时长（天）”
- 更新前端构建压缩包 dist.zip

## 2026-07-20

- OKCIS 招投标爬取改为读取手机版 JSON 截止时间，减少 PC 详情访问
- 手机版 JSON 脱敏或出现“登录后查看”时自动刷新凭证后重试
- 人事考勤汇总去掉“应有年假”，列表和导出改为以本月最新花名册为基础
- 新增花名册上传记录表和应出勤明细表，上传时保存部门、姓名、应出勤和上传时间
- 考勤汇总标题显示最新花名册上传日期，零值统一显示为 `-`
- 考勤汇总表格撑满容器，新增模拟浮动表头，操作列详情去掉图标
- 已在 test/prod 执行花名册上传记录建表 SQL
- 更新前端构建压缩包 dist.zip

## 2026-07-21

- 新增“线上办公助手”工作台卡片，接入 OnlyOffice DocumentServer
- 新增 OnlyOffice 后端入口、编辑器页、文档下载与保存回调接口
- 新增 `app:online_office:access` 权限并默认授权普通用户
- 新增 OnlyOffice test/prod 环境配置，支持独立入口地址、内网回调主机和公网回调地址
- 解决 prod Alembic 历史版本冲突，并保持 test/prod 版本一致
- 修复卡片点击路径和移动端路径，支持跳转独立 OnlyOffice 编辑器页
- 线上办公助手新增文件首页，支持新建 Word、Excel、PPT 文件
- 历史文件支持打开、改名、删除
- 编辑器页顶部支持直接修改文件名并保存
- 开启 OnlyOffice `forcesave`，支持点击保存后通过回调在线落盘
- 确认同一历史文件可多人协同编辑
- 更新前端构建压缩包 dist.zip

## 2026-07-23

- 招投标历史数据导出 tab 权限开放为 `app:okcis_notices:access`，和招标信息公示其它 tab 保持一致
- OKCIS 爬虫支持按订阅组名称 / dzid 包含或排除过滤，并记录详情权限不足 URL
- 新增售前营销 OKCIS 爬取任务配置脚本，支持拆分售前营销和售后招投标爬取任务
- 钉钉调休余额按 `0.1 天 = 1 小时` 口径修正，调休余额换算改为 10 小时/天
- 钉钉考勤同步任务增加临时错误重试，提升定时同步稳定性
- 人事考勤助手多 tab 筛选区调整为同一行布局
- 已执行前端构建、后端相关文件 `py_compile` 校验

## 2026-07-23

- 修复税务助手发票类型历史数据 `0` 展示问题，`1174205` 现为 `普通发票`
- 发票项目管理去掉状态列、详情状态和编辑启用项

- 税务助手新增 `发票项目管理` tab
- `发票项目管理` 支持分类列表、详情、编辑
- 新增税务助手 OA 分类列表、详情、更新接口

- 新增税务助手 OA 项目选项同步表 `tax_oa_project_items`，从 OA `ecology.uf_hwhyslwmc` 每 10 分钟同步项目分类、项目名称、税率
- 取消 `tax_project_sync_items`
- 新增 OA 分类税务信息表 `tax_oa_project_categories`，字段含 OA分类、服务税收编码、税率、项目名称
- `tax_oa_project_items` 新增 `oa_project_category_id` 并回填
- test/prod 当前均为：OA 项目选项 78 条，OA 分类 13 条，全部项目已关联分类ID

- 新增税务助手卡片、独立权限 `app:tax_assistant:access` 与角色 `tax_assistant_member`
- 新增税务助手发票管理页面、后端列表/详情/手动同步接口
- 新增本地表 `tax_invoice_basic_info`、`tax_invoice_detail_info`，并同步数据库字典
- 新增 `tax_invoice_oa_sync` 计划任务，每 3 分钟从 OA 开票申请同步数据
- OA 同步口径限定为未完结且当前最后节点为刘羽丰的开票申请，节点为 `2518 / 6.财务会计`
- test/prod 已完成初始化并同步当前数据：基本信息 24 条、明细 25 条
- 采购部供应商管理补充企业信用编码字段与前端展示
- OnlyOffice 空白 Word/PPT 模板与文件名自动保存体验继续优化
- OnlyOffice 文件名手动保存成功后不再重置自动保存倒计时
- OnlyOffice 文件列表过滤未保存的新建临时文件，保存成功后再标记为已保存
- 已执行前端构建、后端相关文件 `py_compile` 校验

## 2026-07-24

- 税务助手补充发票号回填与审核状态字段
- 审核通过改为 `6.财务会计 / 刘羽丰` 已处理且已有主表或明细发票号
- OA 同步继续跟踪历史单据的流程状态、审批日志、发票号和审核状态
- 发票管理列表、详情、明细展示发票号

## 2026-07-24

- 修复税务助手发票管理列表发票号为空：主表为空时使用明细发票号兜底并回填历史数据

## 2026-07-24

- 新增地下水登记独立公开页面 `/dengji` 和独立接口 `/api/dengji`
- 新增 `groundwater_registry_records` 台账表、模型、Schema、迁移和数据库字典
- 导入天津市地下水登记台账数据，test/prod 均为 1793 条，历史数据默认未填
- 列表仅展示行政区、许可证编号、取水权人、是否已填和操作列，支持移动端卡片
- 编辑页无公共头部导航，保存按钮固定底部，基础信息支持展开查看其他台账字段
- 填写字段支持可输入下拉，输入文字无需额外确认
- 经纬度定位使用手机浏览器坐标，并统一转成度分秒格式显示/保存
- 站点照片支持多张，本地预览后保存时上传；删除已保存照片时同步删除服务器本地文件
- 修复 Alembic revision cycle，test/prod 已升级到 `i2j3k4l5m6n7`
- 已执行前端构建、后端 `py_compile`、`git diff --check` 校验

## 2026-07-25

- 地下水登记首页新增 `是否已填` 筛选，支持全部、已填、未填
- 地下水登记首页新增导出按钮，按当前筛选条件基于原始模板导出最新填写数据
- 原始地下水登记 Excel 保存到项目 `data/groundwater_registry/`
- 新增恢复脚本 `backend/scripts/reset_groundwater_registry_records.py`，可按原始 Excel 重建台账并重置为未填
- 已执行正式环境重置：prod 当前 1793 条、已填 0，本地站点照片清理 4 个
- 已执行后端 `py_compile`、脚本读表校验、前端构建、`git diff --check`
# 2026-07-26 地下水登记筛选联动

- 地下水登记 `/api/dengji/options` 支持按行政区返回镇(街)候选。
- 首页行政区筛选变化后自动清空镇(街)，并刷新对应镇(街)下拉列表。
- 编辑页仍保持全量候选逻辑。
- 已验证：后端 `py_compile`、前端 `./build.sh`、`git diff --check`。

# 2026-07-29 移动首页、徽章管理与考勤修正

- 系统管理新增“徽章管理”，包含徽章配置、用户徽章记录、手动授予、启停、后台统计和数据库字典。
- 移动端 AI 中台首页新增头像、最近访问和排行榜卡片；排行榜当前展示“稳如泰山区域”。
- 新增 `/api/dashboard-rankings`，按一级部门统计离职人数少、招聘人数多的部门排行。
- 修复张辉考勤汇总中 OA 补卡后仍计入 20 分钟内迟到的问题，补卡覆盖的上班/下班时段不再计入迟到早退。
- 已验证：后端 `py_compile`、前端 `cd frontend && ./build.sh`、`git diff --check`。

# 2026-07-29 徽章图标、钉钉缺卡与移动排行榜权限

- 徽章管理默认徽章修正为“勤劳模范”“加班楷模”，新增字标风格大/中/小 SVG 图标并在列表展示。
- 钉钉同步补齐“只有上班或下班一侧打卡”时的缺卡占位，后续真实打卡会覆盖占位。
- 移动端首页排行榜新增独立权限 `app:mobile_dashboard_rankings:access`，无权限默认隐藏且不请求接口。
- test/prod 数据库已升级到 `d6e7f8a9b0c1`。
- 已验证：后端 `py_compile`、前端 `cd frontend && ./build.sh`、`git diff --check`。

# 2026-07-29 钉钉调休重复清洗与移动排行榜隐藏

- 修复钉钉请假同步同时写入假期状态和审批实例导致调休重复扣减的问题。
- 钉钉请假同步改为优先保留假期状态记录，重叠审批实例不再写入本地镜像。
- 新增历史清洗脚本 `backend/scripts/cleanup_dingtalk_leave_overlaps.py`，test/prod 均清理 169 条重叠记录。
- 新增规则：统计、分组、去重、汇总、排行、导出、图表口径等数据处理默认放后端执行。
- 移动端首页排行榜临时隐藏，保留后端接口和权限定义。
- 已验证：后端 `py_compile`、清洗脚本、`git diff --check`。

# 2026-07-30 售前营销看板定时推送

- 新增 `presales_business_dashboard_push` 售前营销业务团队看板推送任务。
- 调度为周一到周五 08:30，默认暂停。
- 接入计划任务管理、手动执行和 cron 执行入口。
- 新增双环境 SQL：`backend/sql/upsert_presales_business_dashboard_push_scheduler_meta.sql`。
- test/prod 均已写入 `scheduler_job_meta` 并确认 `is_paused=1`。
- 已验证：后端 `py_compile`、`git diff --check`。

# 2026-07-30 考勤修正覆盖未打卡统一后端处理

- 宫庆超 2026-07-02 考勤修正覆盖原未打卡口径回到后端统一处理。
- 后端 `checkin_records` 先按 OA 修正方向过滤同槽位原缺卡/未打卡，再返回页面和导出共用。
- 前端去掉重复过滤，只保留展示。
- 已验证：后端 `py_compile`、前端 `./build.sh`、`git diff --check`。

# 2026-07-30 客户端管理与升级接口

- 新增“客户端管理”卡片和独立权限 `app:client_management:access`。
- 新增客户端版本表、Alembic 迁移和双环境建表 SQL，文件保存到 OSS 独立目录。
- 每个应用按 `app_code` 独立版本管理，版本号拆分为外部版本号和内部版本号。
- 新增免登录升级检查接口，按应用编码、平台和内部版本号返回新版本下载地址。
- 客户端管理列表新增部门多选筛选，未限制部门的版本按全部部门处理。
- 已验证：后端 `py_compile`、前端 `cd frontend && ./build.sh`、`git diff --check`。

# 2026-07-31 客户端管理安装包替换与 MD5 校验

- 编辑客户端版本支持替换安装包，替换时覆盖原 OSS 对象地址，保证已分发永久下载链接继续有效。
- 新增安装包 `file_md5` 字段和索引，上传、替换时自动计算 MD5。
- 升级检查接口支持 `current_file_md5`，客户端本地包与服务端最新包一致时返回无需更新。
- 客户端管理文件信息展示 MD5 前 8 位，SQL 初始化脚本和数据库字典同步更新。
- test/prod 均已迁移到 `h0i1j2k3l4m5`，正式库已有客户端包已回填 MD5。
- 已验证：后端 `py_compile`、前端 `cd frontend && ./build.sh`、`git diff --check`。

# 2026-08-06 税务助手发票文件管理

- 新增税务助手“发票文件管理”Tab，支持 PDF/JPG/PNG 批量上传、OCR 识别、文件下载地址复制、重新识别、删除和导出。
- 发票文件使用独立 MinIO bucket：test `tax-invoices-test`，prod `tax-invoices`。
- 新增 `tax_invoice_files` 表，保存上传文件、OCR 结果、匹配状态和企微推送状态；test/prod 已升级到 `l3m4n5o6p7q8`。
- 发票文件列表新增匹配状态、推送状态、匹配项目、申请人筛选和导出字段，操作栏固定右侧。
- 上传弹窗支持拖拽/点击多文件自动上传，逐文件展示进度；上传完成后保持在“发票文件管理”Tab。
- 上传和重新识别均按发票号码判重，重复票号不保存文件、不入库。
- 发票文件管理 Tab 限制系统管理员可见；后端文件相关接口同步加管理员校验。
- 抽出发票文件扫描匹配推送方法，已接入计划任务和手动“扫描推送”按钮；匹配本地未命中时直查 OA `formtable_main_33_dt1.fph`。
- OA 发票同步补抓近期已归档且已有发票号的开票申请；正式库已补齐漏同步的 `1176649 / 26122000001047992611`。
- 已验证：后端 `py_compile`、前端 `cd frontend && ./build.sh`、`git diff --check`。

# 2026-08-07 OKCIS 原始记录本地文件日志

- 新增 `backend/app/services/okcis_raw_record_log.py`，OKCIS 每条原始/enriched 记录按天写入 `backend/data/logs/okcis_raw_records/YYYY-MM-DD.jsonl`。
- JSONL 记录包含 task/run/dzid/page、标题、uniseq、详情地址、原始 record，以及 `raw_record_text/detail_content_text/pc_detail_content_text`，方便直接按日志文件检索。
- OKCIS 爬虫流程改为每页先写本地原始记录日志，再从本页日志读回记录做截止时间筛选和业务入库。
- 运行日志清理任务同步清理 OKCIS 原始记录日志，保留最近 7 天。
- 本地日志排查：`2026-08-06.jsonl` 当前 50 条，未命中指定“深能保定...168小时...交接仪式”项目，仅命中“赵县燃气员工深能之家服务平台采购项目”。
- 已验证：后端 `py_compile`、JSONL 写入/读取、`git diff --check`。

# 2026-08-07 税务发票文件扫描推送计划任务

- 发票文件扫描推送任务 `tax_invoice_file_notification_push` 配置为 5 分钟一次。
- test/prod 两套库 `scheduler_job_meta` 已同步为 `interval_minutes=5`、`is_paused=1`，默认暂停。
- 新增幂等 SQL：`backend/sql/upsert_tax_invoice_file_notification_push_scheduler_meta.sql`。
- 临时将 test/prod 推送人员都固定为康鹏，企微 userid 按现有别名规范归一化为 `tangpeng`。
- 管理后台计划任务说明同步标注“当前临时固定推送康鹏”。
- 已验证：后端 `py_compile`、`git diff --check`。

# 2026-08-07 税务发票推送人恢复正常

- 税务发票文件扫描推送逻辑已恢复为测试环境固定康鹏、正式环境按申请人推送。
- test/prod 两套库任务说明已同步回“测试环境固定推送康鹏，正式环境推送正常申请人”。
- 已确认这套逻辑同时影响手动按钮和计划任务，部署后需要重启 API 和 cron 两个后端进程。
- 已验证：后端 `py_compile`、`git diff --check`。
# 2026-08-24 16:22 项目 Agent 文档迁移

- 建立 `agentic-codex/projects/smart-cs-ai/` 项目目录。
- 迁移本项目 `AGENTS.md`、`CURRENT_CONTEXT.md`、`PROJECT_HISTORY.md`、`PROJECT_HISTORY.archive.md`、`GIT_CHANGELOG.md` 到 agentic 项目目录。
- 根目录同名文件改为轻量入口指针，后续上下文恢复和历史记录优先读取/写入新目录。
- 记录财务报销开票主体中“天津宏捷安装工程有限公司”“晨天润泓（天津）科技服务有限公司”已删除，不再按停用主体处理。

# 2026-08-24 16:30 清理主仓已忽略跟踪文件

- 按 `.gitignore` 清理主仓已忽略但仍被 Git 跟踪的文件。
- 使用 `git rm --cached` 保留本地文件，远端删除对应 md、启动脚本和 package-lock 等已忽略文件。
